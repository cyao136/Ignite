<li class="list-group-item">
  <span class="glyphicon glyphicon-plus"></span>
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <strong><%= link_to activity.owner.username, activity.owner %></strong>
  <% if a.trackable %>
    commented on <%= link_to Project.find(a.trackable.commentable_id).name, Project.find(a.trackable.commentable_id) %>:
    <br><%= a.trackable.body %>
  <% else %>
    added the story that is currently deleted.
  <% end %>
</li>
